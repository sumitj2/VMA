<Window x:Class="VMA.MVVM.Views.SuccessPopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:Conv="clr-namespace:VMA.Converters"
        Title="Notification Popup" SizeToContent="Height" WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent" 
        HorizontalAlignment="Center" VerticalAlignment="Center">
    <Window.Resources>
        <Conv:NotificationTypeToBrushConverter x:Key="NotificationTypeToBrushConverter"/>
    </Window.Resources>
    <Grid RenderTransformOrigin="0,1" >

        <!-- Notification area -->
        <Border BorderBrush="Gray" Width="300" CornerRadius="10" Background="{Binding TypeOfNotification,Converter={StaticResource NotificationTypeToBrushConverter}}" BorderThickness="1" >
            <Border CornerRadius="10" Background="White" BorderBrush="{Binding TypeOfNotification,Converter={StaticResource NotificationTypeToBrushConverter}}" BorderThickness="8 0 0 0">
                <StackPanel Margin="20">
                    <TextBlock TextWrapping="Wrap" Margin="5" FontWeight="Bold" Text="{Binding Header,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}">
                        
                    </TextBlock>

                    <TextBlock TextWrapping="Wrap" Margin="5" Text="{Binding Message,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}">
    
                    </TextBlock>
                    <!--<CheckBox Content="Checkable" Margin="5 5 0 5" />
                    <Button Content="Clickable" HorizontalAlignment="Center" />-->
                </StackPanel>
            </Border>
        </Border>

        <!-- Animation -->
        <Grid.Triggers>
            <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)">
                            <SplineDoubleKeyFrame KeyTime="0:0:0" Value="0"/>
                            <SplineDoubleKeyFrame KeyTime="0:0:0.5" Value="1"/>
                        </DoubleAnimationUsingKeyFrames>
                        <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.Opacity)">
                            <SplineDoubleKeyFrame KeyTime="0:0:2" Value="1"/>
                            <SplineDoubleKeyFrame KeyTime="0:0:4" Value="0"/>
                        </DoubleAnimationUsingKeyFrames>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Grid.Triggers>

        <Grid.RenderTransform>
            <ScaleTransform ScaleY="1" />
        </Grid.RenderTransform>

    </Grid>
</Window>