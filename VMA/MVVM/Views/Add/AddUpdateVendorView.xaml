<UserControl x:Class="VMA.MVVM.Views.Add.AddUpdateVendorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VMA.MVVM.Views.Add"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Border Background="White" CornerRadius="4">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="60"/>
            </Grid.RowDefinitions>
            <Label Content="Add New Vendor Registration" Grid.Row="0" FontSize="14" FontFamily="Montserrat"
FontWeight="Medium" Margin="5 5 0 0"/>
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel" Command="{Binding HideVendorFormCommand}" Style="{StaticResource DefaultButtonStyle}"/>
                <Button Content="Save" Command="{Binding SaveVendor}" Style="{StaticResource DefaultButtonStyle}"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
